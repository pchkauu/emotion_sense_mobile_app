import 'package:flutter/material.dart';

const double designWidth = 375;
const double designHeight = 812;

const designSize = Size(designWidth, designHeight);
const minTextAdapt = true;
const splitScreenMode = true;
