import 'package:flutter/material.dart';

const localeRu = Locale('ru', 'RU');
const localeEn = Locale('en', 'US');

const supportedLocale = [
  localeRu,
  localeEn,
];
const fallbackLocale = localeEn;
const localePath = 'assets/locale';
